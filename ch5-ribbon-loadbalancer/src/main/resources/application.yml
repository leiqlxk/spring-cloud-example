server:
  port: 7777

spring:
  application:
    name: ribbon-loadbalancer
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:8761/eureka/
